channels:
  - conda-forge
  - bioconda
dependencies:
  - snakemake
  - ruamel.yaml
  - semantic_version
  - git # Ensure sunbeam extend works even with tar installation of main pipeline
  - python>=3.10
