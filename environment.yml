channels:
  - conda-forge
  - bioconda
dependencies:
  - bioconda::snakemake
  - ruamel.yaml
  - biopython # Needed for building test data
# - blast # Needed for building test data
  - kraken2 # Needed for building test data
  - semantic_version
 # - git
